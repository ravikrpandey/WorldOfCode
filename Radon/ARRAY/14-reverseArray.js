// Reverse the given array-->

function reverseArray(arr) {

    let result ;

    for (let i = arr.length; i>=0; i--) {
        result = arr[i];
        console.log(result);
    }
    
}

let arr = "ravi is";
// let arr = [1, 2, 3, 4, 5, 6, 7];
reverseArray(arr);